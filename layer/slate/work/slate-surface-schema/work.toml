id = "slate-surface-schema"
title = "Add slate schema and actionable errors"
kind = "build"
status = "complete"
human_request = "Improve slate discoverability with schema output and actionable validation errors"
allium_anchors = ["layer/surface/slate-manager-work-commit-list.md"]
user_alignment = "Directly addresses usability feedback from first external child usage."
belief_refs = []
proof_plan = [
  "[x] `patina slate schema` (or equivalent) shows valid mutable fields and transition gates.",
  "[x] Invalid field errors include valid alternatives and a one-line fix example.",
]
closure_evidence = [
  "Added dispatch `schema` response with mutable set-work fields and lifecycle transition gates.",
  "Improved `set-work` field handling with aliases (`allium_anchors`, `belief_refs`) and actionable invalid-field errors.",
  "Added unit tests for schema discoverability and actionable error text.",
  "Verified with cargo fmt/clippy/test and cargo component build --release.",
]
blocked_by = []
target = "1"
implementation_plan = [
  "Add a discoverable schema surface for mutable fields and lifecycle gates.",
  "Harden validation errors so they suggest valid fields/values and examples.",
]
belief_harvest_decision = "no new belief; reinforced schema/discoverability should be part of the command surface for agent-facing tooling"
created_at = "1778624325"
updated_at = "1778685333"
closed_at = "1778685333"
